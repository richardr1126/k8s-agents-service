---
id: list_capabilities
doc_type: tool
---
List all available workflow capabilities, each with a one-line summary card. Use this
first when you don't already know which workflow fits a user request. After picking a
capability id from the list, call `read_capability(capability_id=...)` to see its full
usage, arguments, and examples before invoking `run_workflow_cli`.
