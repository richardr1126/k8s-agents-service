apiVersion: v1
kind: Service
metadata:
  name: {{ include "agents-service.fullname" . }}
  labels:
    {{- include "agents-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agents-service.selectorLabels" . | nindent 4 }}
