apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agents-streamlit.fullname" . }}-test-connection"
  labels:
    {{- include "agents-streamlit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agents-streamlit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
