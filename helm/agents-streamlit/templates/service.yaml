apiVersion: v1
kind: Service
metadata:
  name: {{ include "agents-streamlit.fullname" . }}
  labels:
    {{- include "agents-streamlit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agents-streamlit.selectorLabels" . | nindent 4 }}
